server:
  port: 8081
#logging:
#  level:
#    ROOT: ERROR
#    '[com.petry.*]': INFO
#  file:
#    path: C:\log\elk-log.log
#  config: classpath:logback.xml

management:
  endpoints:
    web:
      exposure:
        include:
          - health
          - startup
         
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/pdv
    username: root
    password: 120312
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    open-in-view: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
